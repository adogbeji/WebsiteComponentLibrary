This contains code for building a full screen overlay navigation bar


Important Points:

1) A full screen overlay navbar is a trendy way of displaying navbar items
2) However, it should only be used if there aren't too many items or links to display; otherwise,
   it's better to just stick with a normal navbar
3) A full screen overlay navbar consists of two main parts: the overlay and overlay content